{
  "name": "@mteja/ai-engine",
  "version": "1.0.0",
  "private": true,
  "description": "AI engine for profile optimization and message generation using Claude",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.10.2",
    "vitest": "^2.1.8"
  }
}
